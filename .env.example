# GitHub App Configuration
GITHUB_APP_ID=your_app_id_here
# GITHUB_INSTALLATION_ID=your_installation_id_here  # OPTIONAL: Leave blank for automatic discovery
GITHUB_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY-----
your_private_key_here
-----END RSA PRIVATE KEY-----"

# Server Configuration
PORT=4567
BIND_HOST=127.0.0.1            # Use 0.0.0.0 for production
ALLOWED_HOSTS=your-domain.com  # Comma-separated list of allowed hosts
RACK_ENV=production            # Use production to avoid development mode host restrictions
LOG_LEVEL=INFO                 # DEBUG, INFO, WARN, ERROR (INFO for production)
